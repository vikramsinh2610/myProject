const DEFAULT_WIDTH = 792;
const DEFAULT_HEIGHT = 612;

const coreFeatures = {
  invites: 5,
  freeDays: 21,
};

const config = {
  card: {
    defaultWidth: DEFAULT_WIDTH,
    defaultHeight: DEFAULT_HEIGHT,
  },
  authSecret: "mdSelcdjAce3GqvRNTDtknQXJKYZuDfh",
  features: coreFeatures,
};

module.exports = {
  config,
};
